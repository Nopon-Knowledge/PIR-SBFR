base: ../pir_sbfr.yaml
model:
  lambda_scale: 0.0
  lambda_consistency: 0.0
  router:
    # MTF/PSF sharpness descriptor only; relative GSD remains at reference in training.
    physical_fields: [0.0, 1.0, 0.0]
    use_physical: true
    use_visual: false
    p5_bypass: true
